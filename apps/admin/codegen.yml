overwrite: true
schema: "http://localhost:3000/graphql"
documents:
  - "graphql/**/*.graphql"
  - "!graphql/schema.graphql"
generates:
  src/app/graphql/generated/:
    preset: "client"
    presetConfig:
      gqlTagName: "gql"
